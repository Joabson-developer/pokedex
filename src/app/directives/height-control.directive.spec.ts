import { HeightControlDirective } from './height-control.directive';

describe('HeightControlDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightControlDirective();
    expect(directive).toBeTruthy();
  });
});
